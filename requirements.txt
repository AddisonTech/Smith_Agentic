# smith_agentic/requirements.txt
# Install: pip install -r requirements.txt
#
# crewai pulls in langchain, pydantic v2, and litellm as transitive deps.
# litellm handles the Ollama -> OpenAI-compatible API translation automatically.
#
# Tested with Python 3.10+

# Core
crewai==1.14.6
ddgs>=9.0.0,<10.0.0          # renamed from duckduckgo-search
pyyaml>=6.0.1,<7.0.0

# MCP client — connects crews to external OT bridges (Hermes, ignition-mcp).
# See tools/mcp_tools.py + config.yaml `mcp_servers`.
crewai-tools>=1.14.6         # provides MCPServerAdapter
mcp>=1.26.0                  # Model Context Protocol client
mcpadapt>=0.1.20             # adapts MCP tools into CrewAI tools (crewai-tools[mcp])

# Web fetch (WebFetchTool)
requests>=2.31.0,<3.0.0
httpx>=0.27.0,<1.0.0
beautifulsoup4>=4.12.0,<5.0.0
lxml>=5.0.0,<7.0.0           # faster HTML parser for BeautifulSoup

# Memory
chromadb>=0.5.0,<2.0.0       # persistent vector memory

# Real Tools
gitpython>=3.1.40,<4.0.0     # git_tool.py: stage/commit/push

# Web UI
fastapi>=0.111.0,<1.0.0      # REST API + WebSocket server
uvicorn[standard]>=0.29.0,<1.0.0   # ASGI server
websockets>=12.0,<16.0       # WebSocket client support

